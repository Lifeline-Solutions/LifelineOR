<div class="w-full p-6 sm:ml-64">
">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex items-center">
    <h1 class="font-bold text-4xl">Consultations</h1>
    <%= link_to "New consultation", new_consultation_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-black block font-medium" %>
  </div>

  <div id="consultations" class="min-w-full">
    <%= render @consultations %>
  </div>
</div>
